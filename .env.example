# =============================================================================
# Shot Caller VR — Environment Variables
# =============================================================================
# Copy this file to .env and fill in your keys.
#   cp .env.example .env
# Do not commit .env — it is gitignored.
# =============================================================================

# -----------------------------------------------------------------------------
# Pipeline: Google Street View
# -----------------------------------------------------------------------------
# Required for Street View API. Enable "Street View Static API" in Google Cloud.
# https://console.cloud.google.com/apis/library/street-view-image-backend.googleapis.com
GOOGLE_MAPS_API_KEY=

# -----------------------------------------------------------------------------
# Pipeline: Nano Banana 2.5 (Image Expansion via Replicate)
# -----------------------------------------------------------------------------
# Replicate — get token at replicate.com/account
# Model: https://replicate.com/google/nano-banana-2
REPLICATE_API_TOKEN=

# -----------------------------------------------------------------------------
# Pipeline: Nano Banana Pro (Reference Image Generation via Vertex AI)
# -----------------------------------------------------------------------------
# Uses gcloud Application Default Credentials (no API key needed if gcloud is configured).
# Run: gcloud auth application-default login
GOOGLE_CLOUD_PROJECT=ary-pi-5
GOOGLE_CLOUD_LOCATION=us-central1

# -----------------------------------------------------------------------------
# Pipeline: World Labs Marble API
# -----------------------------------------------------------------------------
# Get key at worldlabs.ai
WORLD_LABS_API_KEY=

# -----------------------------------------------------------------------------
# Mastra Intake Layer
# -----------------------------------------------------------------------------
# The intake backend defaults to OpenRouter-routed model IDs. If you keep the
# default models below, set your OpenRouter key.
OPENROUTER_API_KEY=test

# Optional overrides for the local intake runtime
SHOT_CALLER_SERVER_PORT=8787
SHOT_CALLER_INTAKE_MODEL=openrouter/openai/gpt-4.1-mini
SHOT_CALLER_IMAGE_PROMPT_MODEL=openrouter/openai/gpt-4.1-mini
SHOT_CALLER_MESHY_PROMPT_MODEL=openrouter/openai/gpt-4.1-mini
